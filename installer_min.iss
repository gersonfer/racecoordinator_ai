; Race Coordinator AI Minimal Installer Script
; Assumes all external dependencies (JRE, MongoDB) are already installed

#include "installer_base.iss"

[Setup]
OutputBaseFilename=RaceCoordinatorAI_Min_Setup
